import React, { Component } from 'react';

class Navbar extends Constructor {
  constructor(props) {
    super(props)
    this.state = {
      // Default State
    }
  }

  render() {
    return(
      <div>

      </div>
    )
  }
}

export default Navbar;
